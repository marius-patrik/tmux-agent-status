{
  "schemaVersion": 1,
  "installer": {
    "required": false,
    "paths": ["install/install.sh", "install/install.ps1"],
    "postInstallChecks": ["version", "doctor"],
    "notes": "CLI repos should provide platform installers and smoke them in their release scripts. Library, app, and documentation repos may leave installers absent."
  },
  "autoUpdater": {
    "centralized": true,
    "workflow": ".github/workflows/dark-factory-autoupdate.yml",
    "source": "marius-patrik/data-agentos",
    "notes": "DarkFactory performs central managed setup sync through its GitHub App. Per-repo updater workflows verify that managed files are present and current enough to receive updates."
  }
}
