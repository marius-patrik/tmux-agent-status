{
  "schemaVersion": 1,
  "release": {
    "packageManager": "auto",
    "required": {
      "validate": true,
      "build": false,
      "smoke": false
    },
    "scripts": {
      "validate": ["ci", "check", "test"],
      "build": ["build:release", "release:build", "build"],
      "smoke": ["smoke:release", "release:smoke", "test:release"]
    },
    "artifactGlobs": ["dist/release/**", "dist/**"],
    "notes": "Every managed repo must validate before release. Repos that ship binaries or installers should add build and smoke scripts so this managed workflow exercises the actual release path."
  }
}
