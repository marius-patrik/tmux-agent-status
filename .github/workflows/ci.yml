name: CI

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        shell: bash
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          elif [ -f bun.lock ] || [ -f bun.lockb ]; then
            bun install --frozen-lockfile
          elif [ -f package.json ]; then
            npm install
          else
            echo "No package manifest found; skipping dependency install."
          fi

      - name: Validate
        shell: bash
        run: |
          if [ ! -f package.json ]; then
            echo "No package.json found; nothing to validate."
            exit 0
          fi

          if node -e "const p=require('./package.json'); process.exit(p.scripts?.ci ? 0 : 1)"; then
            npm run ci
          elif node -e "const p=require('./package.json'); process.exit(p.scripts?.check ? 0 : 1)"; then
            npm run check
          else
            npm test
          fi
